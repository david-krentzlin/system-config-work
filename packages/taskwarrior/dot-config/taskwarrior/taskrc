# data.location=~/.local/share/task
# default.command=inbox

# pomodoro.defaultFilter=(+next or +doing or +blocked)
# pomodoro.default.sort=project+,urgency-
# pomodoro.prefixProject=1
# pomodoro.prefixSeparator=" - "

# report.inbox.description=GTD inbox
# report.inbox.labels=ID,Project,Prio,Description,Urg
# report.inbox.columns=id,project,priority,description,urgency
# report.inbox.filter=status:pending +inbox


# report.doing.description=GTD doing
# report.doing.labels=ID,Project,Prio,Description,Urg
# report.doing.columns=id,project,priority,description,urgency
# report.doing.filter=status:pending +doing

# report.blocked.description=GTD blocked
# report.blocked.labels=ID,Project,Prio,Description,Urg
# report.blocked.columns=id,project,priority,description,urgency
# report.blocked.filter=status:pending +blocked

# report.completed.description=GTD done
# report.completed.labels=ID,Project,Prio,Description,End
# report.completed.columns=id,project,priority,description,end
# report.completed.filter=status:completed

# report.next.description=GTD next actions
# report.next.labels=ID,Project,Prio,Description,Urg
# report.next.columns=id,project,priority,description,urgency
# report.next.filter=status:pending +next
